Use `AGENTS.md` in the repository root as the canonical instructions file.

For any ABB/SBB work, load and follow the standards referenced by `AGENTS.md`.
Do not infer alternate rules from examples when a standard file states a requirement.
