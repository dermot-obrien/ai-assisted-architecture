# Foundation Mappings

This folder is reserved for cross-domain mapping artefacts that apply across profiles.

Current capability-to-ABB mapping is in:

- `../capabilities/capability-abb-mapping.csv`
